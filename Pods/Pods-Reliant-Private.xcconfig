#include "Pods-Reliant.xcconfig"
GCC_PREPROCESSOR_DEFINITIONS = COCOAPODS=1
HEADER_SEARCH_PATHS = "${PODS_ROOT}/BuildHeaders" "${PODS_ROOT}/BuildHeaders/Reliant" "${PODS_ROOT}/Headers" "${PODS_ROOT}/Headers/Reliant"
OTHER_LDFLAGS = -ObjC ${PODS_RELIANT_OTHER_LDFLAGS}
PODS_ROOT = ${SRCROOT}